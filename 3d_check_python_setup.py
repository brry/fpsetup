# This script tests whether the needed python modules are available.

print("Seeing if modules can be loaded ...")
import numpy
import pandas
import matplotlib
print("It worked! You're good continue in step 4 :)")

# If this fails, go back to Step 3c or 
# press `CTRL` + `SHIFT` + `P`, search "Python: Select Interpreter", 
# set to the path from `3b_install_python.R`

